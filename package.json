{
  "name": "strongstep",
  "version": "1.0.0",
  "description": "StrongStep — Podómetro GPS",
  "private": true,
  "scripts": {
    "build": "echo 'Static app — no build step needed'"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/geolocation": "^7.0.0",
    "@capacitor-community/text-to-speech": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
